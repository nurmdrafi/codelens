# Reference docs — local-only design references, not shipped with plugin
references/*

# codelens review history (per-user, not plugin source)
.codelens/reviews.json
.codelens/reviews.log
.codelens/scan.log

# Hook runtime logs (per-session, not source)
.claude/hooks/.logs/

# Benchmark raw artifacts (regenerable — only keep summaries)
docs/superpowers/benchmarks/reports/
docs/superpowers/benchmarks/transcripts/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/

# Internal documentation (superpowers specs — not shipped)
# docs/superpowers/
