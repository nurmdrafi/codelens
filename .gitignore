# Reference docs (not shipped with plugin) — but report-template.md MUST ship
references/*
!references/report-template.md

# codelens review history (per-user, not plugin source)
.codelens/reviews.json
.codelens/scan.log

# Benchmark raw artifacts (regenerable — only keep summaries)
docs/superpowers/benchmarks/reports/
docs/superpowers/benchmarks/transcripts/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/

# Internal documentation (superpowers specs — not shipped)
# docs/superpowers/
